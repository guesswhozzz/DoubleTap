import React from "react";
import styled from "styled-components";

const NavBarContainer = styled.header`
  display: flex;
`;

export const NavBar = () => {
  return <NavBarContainer>Навбар</NavBarContainer>;
};
