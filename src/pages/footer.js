import React from "react";
import styled from "styled-components";

const FooterContainer = styled.footer`
  display: block;
`;

export const Footer = () => {
  return <FooterContainer>Футер</FooterContainer>;
};
